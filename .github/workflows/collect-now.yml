name: Retired combined collection trial
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Combined trial retired; use source-specific review workflows.'
          exit 1
