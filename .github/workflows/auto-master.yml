name: Retired Auto Master (no collection)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse retired collector
        run: |
          echo 'Auto Master is retired. Use the bounded map or MusicBrainz review workflow.'
          exit 1
