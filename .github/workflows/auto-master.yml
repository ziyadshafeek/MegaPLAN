name: Auto Master — Fully Automatic All Datasets

on:
  schedule:
    - cron: '*/20 * * * *' # Every 20 minutes
  workflow_dispatch:
    inputs:
      batch:
        description: 'Batch size for each scraper'
        default: '10'
        type: string

jobs:
  auto-master:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install
        run: npm ci || npm install
      - name: Run auto master — map + product + music + inception
        env:
          NVIDIA_API_KEY: ${{ secrets.NVIDIA_API_KEY }}
          INCEPTION_TOKEN: ${{ secrets.INCEPTION_TOKEN || '' }}
        run: |
          echo "Running auto master — fully automatic all datasets"
          # Map — Kerala 10-day sprint
          echo "=== Map Kerala ==="
          node scripts/kerala-expansion-runner.mjs --batch 10 --workers 2 || true
          # Product — Amazon Flipkart
          echo "=== Product ==="
          node scripts/product-scraper-runner.mjs --batch 20 --platform flipkart --category mobiles || true
          node scripts/product-scraper-runner.mjs --batch 20 --platform flipkart --category laptops || true
          # Music — Spotify
          echo "=== Music ==="
          node scripts/music-scraper-runner.mjs --batch 30 --query love || true
          node scripts/music-scraper-runner.mjs --batch 30 --query malayalam || true
          # Inception test
          echo "=== Inception ==="
          node scripts/inception-test.mjs http://localhost:4173 || true
      - name: Commit all
        run: |
          git config user.name "auto-master-bot"
          git config user.email "bot@megaplan.local"
          git add data/ public/data/ || true
          if git diff --cached --quiet; then echo "No new data"; exit 0; fi
          MAP=$(jq -r '.totalCells // 0' data/map-directory/index.json 2>/dev/null || echo "0")
          PROD=$(jq -r '.totalProducts // 0' data/product-directory/index.json 2>/dev/null || echo "0")
          MUSIC=$(jq -r '.totalTracks // 0' data/music-directory/index.json 2>/dev/null || echo "0")
          git commit -m "auto-master: fully automatic map ${MAP} cells, product ${PROD}, music ${MUSIC} [skip ci]"
          git push origin HEAD:${{ github.ref_name || 'main' }}
      - name: Summary
        run: |
          echo "### 🤖 Auto Master — Fully Automatic" >> $GITHUB_STEP_SUMMARY
          echo "Map: $(jq -r '.totalCells // 0' data/map-directory/index.json) cells" >> $GITHUB_STEP_SUMMARY
          echo "Product: $(jq -r '.totalProducts // 0' data/product-directory/index.json) products" >> $GITHUB_STEP_SUMMARY
          echo "Music: $(jq -r '.totalTracks // 0' data/music-directory/index.json) tracks" >> $GITHUB_STEP_SUMMARY
          echo "Free storage: GitHub + Vercel + IndexedDB, fully indexable for AI" >> $GITHUB_STEP_SUMMARY
