name: Authorized product feed readiness
on:
  workflow_dispatch:
jobs:
  readiness:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'No nationwide product feed is configured. Request retailer/affiliate permission first.'
          echo 'Import an authorized JSON feed locally with scripts/import-authorized-products.mjs; publish only verified records.'
